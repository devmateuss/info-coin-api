# setup.cfg
[pytest]
testpaths = app/tests
addopts = --strict-markers
python_files = test_*.py
python_functions = test_*
python_classes = Test*
log_level = INFO

[options]
# Adiciona a pasta `app` ao caminho do Python para permitir as importações
packages = find:
